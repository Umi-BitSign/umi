# Loaded after the host's normal containers.conf files.  The UMI worker does
# not require any default sysctls, and disabling them keeps rootless Podman
# compatible with hosts that make /proc/sys read-only for the supervisor.
[containers]
default_sysctls = []
